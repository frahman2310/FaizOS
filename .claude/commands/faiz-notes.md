---
description: Flip through your revision notebook (also a spaced review of past lessons)
---
1. Call `faizos_notes`.
2. Show the most recent revision notes compactly — each as its **topic** + its one-line **Remember**. Tell him the full compiled notebook lives at the returned `notebook_path` (`notebook/REVISIONS.md`).
3. Optional spaced review: pick one *older* note and ask him to recall its key rule from memory before you show it (retrieval, not re-reading).
4. Finish by showing the course progress bar from `faizos_progress` (its `rendered` block), then point him back to building.
