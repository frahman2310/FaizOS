---
description: This week's frontier ingest, grouped by the track it affects.
---
1. Call `faizos_frontier_ingest` if it has not run this week (it fetches; you summarise).
2. Render the recent `frontier` rows grouped by affected track: title, one line summary, URL.
3. Flag any track whose `current_as_of` has drifted more than 60 days: name it and offer to
   refresh that track's frontier notes now.
4. For items affecting the CURRENT track, say in one line what changes about what he is
   building. Skip items for tracks more than two positions away.

Keep it under twenty lines. The frontier is context, never the session.
