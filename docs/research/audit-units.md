# Audit: the first 8 units (llm-01 to llm-04, code-01 to code-04)

Written 2026-09-24. An independent, adversarial check before Faiz sees any unit. Nothing in the units was
changed. Checked against `structures/llm-behaviour.md`, `structures/code.md`, `structures/INTEGRATED.md`
(wins on conflict), `structures/review.md`, `learn/facts.md`, every file in `learn/runs/`, the expert sources
in `private/research-base/`, `docs/learning-evidence.md` and `docs/research/postmortem.md`.

**Resolution (2026-09-24, same day):** all 8 units were revised against every finding (both blockers: the
temperature-0 claim in llm-03 now quotes Anthropic's glossary, and llm-04 shows the hidden "You are Qwen"
line the model was sent). All 8 pass `learn/check_unit.py`; all 79 code runs re-run with identical output;
long quotations checked against the saved sources.


**What was run.** All 27 programs in `learn/code/` with python3 3.14.6, plus the three tests with a
file argument. All 29 saved code runs reproduce exactly (output and exit code). Every planted bug runs
to the end with a wrong result (quiet), and every "wrong" Parsons version fails its test. Both Parsons
items have exactly one passing order. All cost numbers were recomputed (see the table). `check_unit.py`
passes all 8 units; the faults below are the kind it cannot see. Two claims were checked by running the
local tokenizers: o200k's split rule and the Qwen chat template (`learn/.venv`).

**Counts: 2 blockers, 22 major, 29 minor (53 findings).**

## Findings

| # | Unit | Step | Severity | Problem | Evidence | Fix |
|---|---|---|---|---|---|---|
| 1 | llm-03 | Wrong idea fixed, Close Key, Card 2 | **blocker** | Teaches the misconception the spec says this concept must break. "Temperature 0 gives the same answer each time"; Card 2: "so the same question gives the same answer"; Close Key: "low means repeatable". | Anthropic glossary (`anthropic-glossary.md` l.66): "Even with temperature set to 0, the results will not be fully deterministic and identical inputs may produce different outputs across API calls." Spec llm-behaviour.md concept 3 lists "temperature 0 is deterministic" as a wrong model to break; refutation item C; LBCI item 2 (Thinking Machines: 80 distinct outputs in 1,000 runs at T 0). The local run is greedy (`do_sample=False` in `demo.py`), so it is deterministic on this Mac only. | Say: "at 0 the model takes the likeliest piece; on this Mac that repeats exactly; on a provider's servers it usually repeats but is not guaranteed (Anthropic glossary)". Rewrite Card 2 and the Close Key. Add a boundary card: "Name one reason T 0 output can still change between API calls". |
| 2 | llm-04 | Odd result, Pick and say why Key, Wrong idea fixed, Card 5 | **blocker** | "Nothing had been said earlier" and "a fresh call made up the name 'Qwen'" are false. `demo.py chat()` applies Qwen's chat template, which inserts the system prompt "You are Qwen, created by Alibaba Cloud. You are a helpful assistant." The model copied the name "Qwen" (and Alibaba's home city, Hangzhou) from text that **was** sent. Option C ("none was sent about a name or city") is also not quite true. Card 5 puts a false fact into the recall queue. | `learn/demo.py` `chat()`; the template, rendered with the Qwen tokenizer, starts `<|im_start|>system\nYou are Qwen, created by Alibaba Cloud...`; the same prompt is visible in `runs/llm03-fruit-chances.json`. | Show the full text sent, system line included. This proves the unit's point better: "it answered from the only text it had, a hidden line calling it Qwen". Fix option C to "none was sent about **your** name or city". Delete or rewrite Card 5. |
| 3 | llm-01 | Odd result, Wrong idea fixed, Card 3 | major | The claim that row 3 "missed" a fact it had just been told is not supported. " located" is a correct continuation ("...is located in Islamabad"). The run captured only the top 5 **first** pieces, never a completion. The refutation leans on it ("missed straight after being stated. A search would not behave like that"). Card 3 turns it into a general rule. It also clashes with Card 4 and the Key: the fix taught is to put the source in the prompt. | `runs/llm01-capital-after-told.json`: located 32.4%, the 10.8%, a 7.0%, in 3.8%, situated 3.3%; no generation run exists. It is a 0.5B model; putting facts in context is the basis of retrieval (the unit's own Key). | Either run a greedy completion of row 3 and show what it says, or drop the "told and missed" framing and refute with rows 1 vs 2 (wording) only. Drop or rewrite Card 3. |
| 4 | llm-01 | Explain Key | major | The quoted source is about a different kind of model. Karpathy's "document completers" describes **base** models before fine-tuning (State of GPT l.47). The demo is Qwen2.5-0.5B-**Instruct**, a fine-tuned assistant, fed raw text without its chat format. "is just a sequence of tokens" (l.75) is about training on a sentence, not about completion. The two phrases are stitched together as one claim. | `karpathy-state-of-gpt-transcript.md` l.47, l.75; `demo.py` MODEL = Qwen2.5-0.5B-Instruct; `next_tokens()` sends raw text with no template. | Either run the rows on the base model (Qwen2.5-0.5B), or say "sent as plain text without the chat format, the model behaves like a document completer". Quote one passage exactly, with its context. |
| 5 | llm-01 | all steps, Cards 1-2 | major | The unit says "next word" throughout, and Card 1 says "a chance to every possible next word". llm-02's refuted idea is "a token is a word". The permanent cards will contradict the next unit. | `runs/llm01-capital-plain.json` shows pieces such as " ______"; llm-02 wrong idea and Card 1. | Use "next piece of text" (as llm-03 does) or say once "a word, or part of one; unit 2 explains". |
| 6 | llm-02 | Pick and say why (option C), Explain Key, Wrong idea fixed | major | Digit splitting is explained by rarity ("rarer text is cut into several"), but for numbers it is a fixed rule. o200k's split pattern has `\p{N}{1,3}`: any run of digits is cut into groups of at most 3 (1000000 becomes 100 / 000 / 0). Qwen cuts every digit alone, even "2024" and "1000". The "1234567" row, the unit's main evidence, is not caused by frequency. | Ran in `learn/.venv`: o200k `_pat_str` contains `\p{N}{1,3}`; Qwen tokenizes "1000 2024 99" as single digits. | Keep rarity for words and scripts. Add one line: "numbers are cut by a fixed rule: OpenAI's in groups of 3, Qwen's one digit each". Adjust the C Key and "The right idea". |
| 7 | llm-02 | Wrong idea fixed ("What it costs") | major | "That $1,000 becomes about $1,300 after switching" ignores the price per token. Moving from Sonnet 4.6 ($3/$15) to Sonnet 5 ($2/$10) gives 1.3 x 2/3 = 0.87, so about **$867**, a fall. The $1,000 bill is never said to be on Claude. The $1,500 and $2,700 are scaled from **one** 10-token sentence, input only; "at Qwen's rate" sounds like a price, but no one bills in Qwen tokens. | facts.md price table; `anthropic-token-counting.md` l.1414: "don't reuse token counts measured on the older model to estimate costs". Recomputed: 1000 x 15/10 = 1500; 1000 x 27/10 = 2700; 1000 x 1.3 = 1300; 1000 x 1.3 x 2/3 = 866.7. | Say "at the same price per token, +30% tokens is +30% cost; the new models also cost less per token, so count and price together". Mark the Urdu ratios as coming from one sentence, "if your real messages show the same ratio". Replace "Qwen's rate" with "Qwen's ratio". |
| 8 | llm-03 | Pick and say why Key vs Explain Key and title | major | It contradicts itself on what temperature changes. Pick Key: "the chances did not change, Apple stayed at 23.3%; only the picking rule changed". Explain Key and title: "Temperature reshapes the chances... makes the likeliest piece even likelier". Both use the word "chances" for two different things, which is exactly his E4 error (which value a name holds). | Unit lines 38, 77, title; Huyen l.43: temperature "redistribute[s] the probabilities". | Name the two things: "the model's chances" (fixed by the model) and "the picking chances" (after temperature). Use the same pair in the title, Keys and Cards. |
| 9 | llm-03 | Odd result, Wrong idea fixed, Card 3 | major | "A 23.3% favourite" and "Apple at a 23.3% chance" treat the first **piece** "Apple" as the whole answer Apple. "App" (8.8%) and " App" (4.0%) also start Apple, so Apple as an answer is at least 36%. The point (well under half) survives, but the number is used in the wrong sense and goes onto a card. | `runs/llm03-fruit-chances.json`: Apple 23.3, Orange 12.6, App 8.8, Ban 5.0, " App" 4.0, Str 3.4. | "Its likeliest first piece, 'Apple', had 23.3%", or sum the pieces that begin Apple (≥36.1%) and say so. |
| 10 | llm-03 | Run, New case and Key | major | It generalises junk output at T 1.5 to AI apps in general. The runs use a 0.5B model with **no top-k or top-p cut-off** (`top_k=0, top_p=1.0` in `demo.py sample`). Provider defaults usually cut off the rare tail, and larger models spread their chances less. The New case asks about "the app" as if 1.5 would give "Jakarta Mexico" in production. | `demo.py` `sample()`; Huyen l.61 says higher temperature makes output "potentially less coherent", not broken; demo.py docstring: "its behaviour shows the mechanism, not the quality of frontier models". | Add one line in Run: "a tiny model with no cut-off shows the effect at its most extreme". In the New case Key say "more wrong or off-topic answers", not "junk". |
| 11 | LLM, all 4 | New case | major | The transfer question has 2 options, and the wrong one is the idea refuted two steps earlier, sometimes almost absurd: "looked up the multiplication table and misread one digit" (llm-01); "will name more real cities" right after he saw junk (llm-03); "the model learned about the project and stored it inside itself" (llm-04). It tests recognition, not transfer. His record: "Option C obviously (decisions cannot be this obvious)" (C35); questions must make him think (C36, C38). | Spec: the transfer revote is a ConcepTest with distractors from real wrong models, on a new surface. | Use 3-4 options, each a plausible wrong model that is **not** the one just refuted (e.g. llm-04: "the app fine-tuned a copy of the model on his chats"). |
| 12 | LLM, all 4 | whole unit | major | No step tells him the demo model is a 0.5B local model and which results are small-model artefacts: the row-3 "miss", 9503, the junk at 1.5, the invented name. The task asked for exactly this check; the only caveat sits in `demo.py`'s docstring, which he never sees. | `demo.py` docstring; findings 3, 10; Karpathy deep dive l.211: frontier models "now get it correct" on strawberry. | One fixed line in every Odd result: "a tiny model (0.5 billion numbers, frontier models are hundreds of times larger); it shows the mechanism; large models make this mistake less often". |
| 13 | LLM, all 4 | Wrong idea fixed ("What it costs") | major | The spec's step 6 is "works out one money or risk number". In llm-01 and llm-03 no number is worked at all (the "1,000 a day" given is unused). In llm-02 and llm-04 every number is computed for him, so he only reads them back (C36; telegraphing, B12). | llm-behaviour.md sec. 2, step 6; learning-evidence C36. | Give the inputs and ask for the number: llm-04 "cost per chat at $2/M"; llm-02 "the Urdu bill at 1.5x"; llm-03 "wrong answers a day if 5% of askers use that wording". |
| 14 | All 8 | none (missing) | major | No unit asks him to predict his score before a check. This is shared-layer rule 2 of INTEGRATED ("Before every check he predicts his score") and postmortem sec. 5.6. The engine records `--predicted` at the end, but nothing asks for it before the check. | INTEGRATED sec. 2.2; code.md step 2; `engine.py cmd_done`. | Add one line at the top of Predict (code) and Pick and say why (LLM): "Before you start: your predicted score for this unit, out of 100." |
| 15 | llm-01, llm-04, code-04 | file names, titles | major | `engine.py today` prints the unit's path to him ("Unit: llm → units/llm/01-it-predicts-it-does-not-look-up.md"). The name answers Pick and say why in llm-01; "04-no-memory" answers it in llm-04; code-04's "quietly-wrong" and its title give the label of every item in the unit (see 22). | `engine.py` `cmd_today` prints `f.relative_to(HERE)`. | Use neutral file names and titles (e.g. "01-capital-of-pakistan"), or print only the unit id. |
| 16 | code-01 | Predict Key, Card 1 | major | "A missing dict label is always a crash, never a quiet wrong" and "always stops the program" are false. `RATES.get("sonnet")` returns None quietly; a KeyError inside `try/except KeyError` or `except Exception` is swallowed. E1, crash vs quiet, is exactly the distinction this card trains, and code-02 teaches try/except next. | Python semantics; his E1 list includes "missing dict key (said 'nothing')". | "`RATES['sonnet']` with no such label raises KeyError, and the program stops unless something catches it." |
| 17 | code, all 4 | Trace | major | The memory tables leave out names, so they are not the full table the spec asks for ("every name, every row"; Cunningham: an incomplete sketch is as bad as none). code-01 has no `prompt`, `model`, `text` or `rate`. code-02 has no `script`, whose shrinking **is** the mechanism, and no `text` or `err`. code-03 has no `case` or `note` before the if. code-04 folds three loops into one row per record, which is not the order the program runs in, and the "retried after (d)" cell is undefined for record 1, where (d) does not run. | code.md step 3 and the Xie and Cunningham rows; INTEGRATED Code bar "90% of trace cells". | Add the missing columns (at least `script` in code-02, `text` in code-01). Give code-04 one row per line that runs, in order. |
| 18 | code, all 4 | Trace | major | There are no subgoal labels. The spec says "the real 15 to 30 line worked example, with subgoal labels as comments", and code.md sec. 3c lists them per task class (T1 "build request, send, check status, pull fields"; T2 "set start values, try, record, wait, give up"). The units use (a)-(f) markers only. | code.md step 3, Margulieux rows, 3c table. | Add the class's labels as comments on the first two units of each pattern; fade them from the third (spec). |
| 19 | code, all 4 | Find the bug | major | The debug card is written by the tutor in the Key; he does not use it. The spec wants him to fill in symptom, **suspect lines**, hypothesis, **predicted check result**, run, fix, **rerun tests**, **undo failed fixes**. The step asks only for a hypothesis and one line. He cannot run a check, and the Key cards leave out suspect lines, the predicted result and rerunning the tests. | code.md step 6 and the Michaeli-Romeike and Ko rows (d = 0.69 came from using the process). | Make the card the step: blank card fields; he names a check ("print X"); the tutor runs `bug.py` with that print and returns the output; then the fix and the test rerun. |
| 20 | code, all 4 | Cards | major | Code cards repeat the unit's exact code (`RATES["sonnet"]`, `times.sort`, `"$100" in case`). INTEGRATED 2.1: "number and code items always come back with new numbers or new code, never the same surface". Cards that stay the same get recognised, not reasoned out. | INTEGRATED sec. 2.1; the review's early-warning measure (high in-session scores, low 7-day cold scores). | Write each code card as a template with a new surface at each review (another dict, label and list), or mark them for generation by the queue. |
| 21 | code-03 | Find the bug | major | The bug needs a rule the unit never states: `in` on a dict checks only its labels. The Trace states `in` only for two pieces of text. Question 1 ("what is it checking instead?") tests that unstated rule. His record: 6+ misses on rules never stated, then tested (E7, B6). | Unit l.48 (only the text rule); learning-evidence E7. | State the dict rule in Trace or Predict (one line plus one tiny example), or ask only "which sticker should the check look inside?". |
| 22 | code-04 | Trace | major | He must fill a table for **broken** code. His record: simulating broken code in a fill-in table went 0 of 4, while tables of working code went 5 of 6 (B11, which lists "`log = []` in loop" and "misplaced stopwatch"). | learning-evidence B11. | Trace the working version (`change.py`) in the table, then ask for the label and "which number changes" on the broken one; or show the table and ask him to find the wrong cell. |
| 23 | code-04 | whole unit | major | Every item's answer is "quietly wrong": the title says so, the Trace intro says "one number it prints is wrong", and every bug step says "it runs with no error". So the crash / quietly wrong / fine choice (E1, the unit's main target) is never really made. | Unit title; Predict, Trace, Find the bug. | Mix the labels: make one Predict line a crash and one fine, and use a neutral title. |
| 24 | code-04 | Predict | major | The item needs two rules that are never stated: a method runs only with round brackets, and `.sort()` sorts the list in place. "Square vs round brackets" is on his list of rules never stated and then tested (E7). The rule appears only in the Key. | learning-evidence E7, B6; "Taught" ledger has no `.sort()`. | Teach `times.sort()` on a 3-item list first (one tiny example), then the item; or give the item after a unit that uses `.sort()`. |
| 25 | llm-01 | Pick and say why Key | minor | The D reason is garbled: "row 3 has no question mark and fails, but so does the lack of an answer-shaped context". | unit l.40 | "Row 4 has no question mark and still gives Islamabad first" (shown later), or "row 1 and row 3 differ in context, not punctuation". |
| 26 | llm-01 | Odd result | minor | "______ 6.0%" is shown as a "next word" with no word about what it is (a fill-in-the-blank line from test papers). | `runs/llm01-capital-plain.json` " ______" | Add "(a blank line, as in a fill-in exercise)". |
| 27 | llm-01 | Pick and say why | minor | Option C ("does not know the capital") is half true under the unit's own thesis (there is no store of known facts), so it is ambiguous. | Card 1 | "C. The model has never seen the capital of Pakistan in its training text." |
| 28 | llm-01 | Card 4 | minor | Not atomic: two answers, and "test many wordings" does not make answers depend less on memory. | Matuschak and Wozniak rules (spec sec. 5, rule 2) | Split the card; keep "hand it the source text". |
| 29 | llm-02 | Card 3 | minor | Karpathy gives two causes (tokens **and** weak counting) and says current frontier models now answer 3. | deep dive l.211-213 | "...partly because they see tokens, not letters (Karpathy); counting is also weak." |
| 30 | llm-02 | Card 5, Wrong idea fixed | minor | "Newer Claude models" is vague and the question depends on its wording. | facts.md: Claude 4.7 and later (Sonnet 5, Opus 5, 5.5) vs Sonnet 4.6 and earlier | Name the boundary: "Claude Opus 4.7 and later vs earlier". |
| 31 | llm-02 | Odd result | minor | "OpenAI's (used by GPT models)" is unsupported for today's GPT models; o200k is the GPT-4o-era encoding and facts.md lists gpt-6 models. | facts.md; the tokenizer name in the run | "OpenAI's public o200k tokenizer (GPT-4o era)". |
| 32 | llm-03 | Run Key | minor | "4 different" at 0.7 counts "Apple", "Apple." and "Apples." as different answers; there are really 2 (Apple, Orange). | `runs/llm03-fruit-temperatures.json` | Say "4 different strings, 2 different fruits". |
| 33 | llm-03 | Odd result | minor | The question gives away the observation it asks for ("A 23.3% favourite won 10 times out of 10. What is the first thing that strikes you?"). | B12 telegraphing ×4 | End at the table and the "all 10 were Apple" line. |
| 34 | llm-03 | Card 3 | minor | Not atomic: two runs in one answer (Apple and 9503). | card rule 2 | Keep one (9503 is the stronger one: repeated and wrong). |
| 35 | llm-04 | Wrong idea fixed | minor | The cost given is ambiguous. "20 exchanges of about 300 tokens" is computed as turn k sending k x 300, so each exchange is the new message and replies are never re-sent. The "only the new message" option could not work (the model would lose the chat). Output tokens are left out. The numbers themselves are right: 63,000; 6,000; 10.5x; $0.126; $0.012; $126; $12. | `runs/llm04-resend-cost.json`, recomputed | "given: 20 messages of 300 tokens; replies not counted". Call 6,000 "a lower bound that could not work", not an option. |
| 36 | llm-04 | Predict | minor | The outcome is almost certain (the answer is in the prompt), so the prediction carries little information. The spec aims for items right 35-70% of the time. | llm-behaviour.md, ConcepTest band | Predict something uncertain, e.g. "fact 20 lines up, in a long message: does it still answer?" |
| 37 | llm-04 | Wrong idea fixed Key | minor | The Key expects prompt caching and summarising, which were never taught, and "prompt caching" is not explained (E7; C33). | Key l.96 | Accept any "send less" answer; explain caching in one line if it is shown. |
| 38 | llm-04 | all steps | minor | "Call" is used as the core term and never defined in the unit (C1, C33). | unit text | "a call (one request to the model and its reply)" at first use. |
| 39 | LLM, all 4 | Pick and say why | minor | Answer-position and length cues. The right answer is never A (B, C, D, C); the targeted wrong idea is A in 3 of 4; the right option is the longest in llm-02, llm-03 and llm-04. | the four units | Rotate positions; make option lengths similar. |
| 40 | LLM, all 4 | Cards | minor | No card names its source file (spec card rule 7); only llm-02 Card 5 names facts.md. | llm-behaviour.md sec. 5, rule 7 | Append "(source: file)" to each card. |
| 41 | LLM, all 4 | Odd result | minor | "What is the first thing that strikes you?" with the Key "Any honest observation" cannot be marked, and his record asks for analytical questions (C36, C38). | learning-evidence C36, C38 | Ask a scoreable question: "Which row gives Islamabad the highest chance, and what is different about its wording?" |
| 42 | code-01 | Predict, Trace | minor | `1_000_000` (a number with underscores) is never explained (E7). | unit l.19 | Add "`1_000_000` is one million; the underscores are only for reading". |
| 43 | code-02 | Trace Key, Card 4 | minor | "The wait gets longer each time" is wrong for the last item (0.0). Card 4 holds two facts (waits and the number of tries). | `BACKOFF = [0.5, 1.0, 0.0]` | "Longer after try 1 and 2; no wait after the last try." Split the card. |
| 44 | code-02 | Find the bug | minor | "1 call was made", given "from a real run", is a hard-coded `print("calls made:", 1)`. | `code/code-02/bug.py` l.29 | Count calls in the program, or drop "from a real run" for that fact. |
| 45 | code-02, code-04 | Trace, Find the bug | minor | Same surface as items he has already been taught and answered: the L3 retry loop with `BACKOFF = [0.5, 1.0, 0.0]` and "529 overloaded", the failure record inside the loop, the misplaced stopwatch (ledger "Taught... do not re-teach"; D7). This invites recognition (C27; INTEGRATED "new surfaces"). | learning-evidence ledger and D7 | Keep the E2 and E3 targets, but on a new surface (another list, other waits, another record). |
| 46 | code-02 | Change | minor | Returning two values (`return x, attempts`) and the printed `('reply to: hi', 3)` are never explained. | unit l.111, l.133 | One line: "two values come back together in round brackets". |
| 47 | code-03 | Trace | minor | He must write `pass rate 75%` from `f"{...:.0f}%"`, and the f-string rule and tuple unpacking (`passed, failing = score(...)`) are never explained (E7). | unit l.75, l.78 | Explain both in one line each, or give the third print line. |
| 48 | code-03 | Change Key | minor | "Watch also for E before C but indented inside the loop" cannot happen: the step says the indentation is already right, and E is fixed at 4 spaces. | unit l.109, l.133 | Delete the sentence. |
| 49 | code-03 | Predict | minor | 9 code lines; the spec caps the tiny version at 8. | code.md step 2 | Drop one print. |
| 50 | code-03 | Find the bug Key, Card 5 | minor | "The fall would be 0" uses a gate term without explaining it; Card 5 ("first suspect") is vague. | unit l.162, l.184 | Explain "fall = baseline minus new score", or drop the gate.py sentence. Make Card 5 precise. |
| 51 | code-03 | Trace | minor | Markers (a)-(e) are in the code but the table does not use them. | unit l.65-72, l.83 | Use them, or remove them. |
| 52 | code-04 | Change | minor | The fix was already given in the Trace Key ("walks `log` instead of `oks`"), so Change repeats it with no new work (C22, C27). | unit l.89 | Make Change a new edit (e.g. add "failed" to the report), tested. |
| 53 | code-04 | Cards | minor | Card 5 is a list card (spec: "avoid sets"); it names 3 places while E1-E4 are four. The header claims E3, but the unit has no per-call vs per-try item. | card rule 3; header "E1, E2, E3, E4 mixed" | Split into single-fact cards; fix the header. |

## Numbers checked (all match their run or facts.md unless listed above)

llm-01: 21.9, 8.1, 6.0, 5.6, 5.5 / 81.9, 10.3, 1.6 / 32.4, 10.8, 7.0 / 45.0, 12.5, 7.8, 5.1, 1.8; 347 x 29 = 10063, model 9503.
llm-02: 9/10/10 words and tokens; 1234567 → 3 and 7; strawberry 3 and 3; Urdu 15 and 27; Roman 16 and 16; withholding 7; ratios 1.5 and 2.7; bills 1,500, 2,700, 1,300 (arithmetic right, meaning wrong: #7).
llm-03: 23.3, 12.6, 8.8, 5.0; distinct 1/4/10; 0.7 split 5/3/1/1; 9503 x5; city distinct 9.
llm-04: 63,000; 6,000; 10.5; $0.126; $0.012; $126; $12 (recomputed with python3).
code: 0.0027, 0.0063 (1200 x 1 + 300 x 5 = 2700; 300 x 1 + 1200 x 5 = 6300); 300.0 ms; attempts 3; 3 records / 2 failed; 3 of 4, 75%; 0 of 4; 0.0054; retried 3 vs 2; spend 0.0027 / 0.0054 / 0.0054 / 0.0081; 950 vs 200; 2400 = 0.3 + 0.5 + 0.3 + 1.0 + 0.3.

## Spec alignment summary

| Required element | llm-01 | llm-02 | llm-03 | llm-04 |
|---|---|---|---|---|
| Puzzle of real odd results | yes | yes | yes | yes (but #2) |
| Concept question, answer + reason + confidence | yes | yes | yes | yes |
| Committed prediction with a number | yes | yes | yes | yes (weak, #36) |
| The run | yes | yes | yes | yes |
| Explain vs a quoted, named source | quote mismatched (#4) | yes | yes | yes |
| Refutation naming the wrong idea, using his run | yes (#3) | yes | teaches a wrong idea (#1) | false evidence (#2) |
| Money or risk number he works out | no (#13) | given to him | no | given to him |
| Transfer on a new surface | 2 options, giveaway (#11) | 2 options | 2 options, giveaway | 2 options, giveaway |
| 4-6 atomic cards | 4, one not atomic | 6 | 5, one not atomic, one wrong | 6, one false |
| Skip rule | yes | yes | yes | yes |
| Score prediction (INTEGRATED) | no | no | no | no |

| Required element | code-01 | code-02 | code-03 | code-04 |
|---|---|---|---|---|
| Predict with crash / quietly wrong / fine | yes | yes | yes | yes (labels all the same, #23) |
| Full memory-table trace | partial | partial (no `script`) | partial | broken code, merged loops (#22, #17) |
| Subgoal labels | no | no | no | no |
| One-sentence purpose | yes | yes | yes | yes |
| Parsons with distractor, or 1-5 line edit with test | edit, test | Parsons, unique | Parsons, unique | edit, test (repeats Trace) |
| Find the bug, quiet, with debug card | yes, card incomplete | yes, card incomplete | yes, card incomplete; rule unstated | yes, card incomplete |
| Cards | 5, fixed surface, one wrong | 5, fixed surface | 5, fixed surface | 5, fixed surface, one list |
| Score prediction (INTEGRATED) | no | no | no | no |

## Verdict per unit

- **llm-01: not ready (3 major, 4 minor).** The core contrast (question wording 81.9% vs plain 21.9%
  "located") is sound and well chosen. The "told and missed" argument and Card 3 go beyond the run. The
  Karpathy quote describes a base model, and "next word" will clash with llm-02. Fix #3, #4, #5, #11, #12.
- **llm-02: not ready (2 major, 3 minor).** Strong runs, verified quotes, good Urdu and Roman Urdu surface.
  Fix the digit mechanism (#6) and the Claude cost claim (#7) before use. The $1,300 figure is the kind of
  wrong number the fact sheet was built to prevent.
- **llm-03: blocked.** It installs the exact misconception the spec assigns this concept to break
  (temperature 0 is deterministic), on a card. It also contradicts itself on what "the chances" are. The runs
  are good; the text needs a rewrite of the right idea, Card 2 and the Close.
- **llm-04: blocked.** The odd result is misread: the hidden Qwen system prompt supplied "Qwen". Once
  that is shown honestly, it becomes the unit's best evidence. The cost step is correct but needs a clearer
  given.
- **code-01: ready after one fix (#16) plus the cross-unit fixes.** The code, runs, Keys and test are all correct.
- **code-02: ready after cross-unit fixes.** Best of the eight: E2 Predict, E3 Trace, a Parsons item with one
  passing order and a real indentation distractor, and a quiet bug that reproduces. Add `script` to the table
  and move it to a new surface if it repeats L3 too closely.
- **code-03: not ready (1 major, 5 minor).** The bug is well planted (the `note` made and never used), but it
  tests the dict `in` rule, which is never stated. Also explain the f-string.
- **code-04: not ready (3 major, 3 minor).** Its Trace uses his worst-measured format (broken code in a table,
  0 of 4). Its title and framing make every label "quietly wrong". The Predict needs a bracket rule he was
  never taught. Change repeats the Trace answer.

**Cross-unit, before any unit is sent:** add the score prediction (#14), neutral file names (#15), a one-line
small-model caveat in every LLM unit (#12), harder transfer options (#11), and subgoal labels, full tables,
a debug card he fills in, and new-surface code cards in every code unit (#17-#20).
