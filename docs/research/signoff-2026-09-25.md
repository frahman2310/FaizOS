# Final sign-off before teaching resumes (2026-09-25)

Independent and adversarial. Nothing was edited except this file. Every earlier report was treated as a
claim to test. Engine and guard walks ran in scratch copies (the scratchpad `signoff/`, `code02/`, `llmprod/`,
`evaldesign/` folders), never against the real data, except one read of `learn/data/`.

## Verdict

| Unit | Verdict |
|---|---|
| code-01 Reading an AI reply | **READY once blocker X1 is cleared** (one file deleted) |
| code-02 One call, one record | READY; fix major N1 before its Retry can ever be served |
| llm-01 It predicts, it does not look up | READY; major L1 (no room for a slip) |
| llm-02 Tokens | READY; major L1 (no room for a slip) |
| production-01 What one call costs | READY |
| evaluation-01 Does the recipe fit the diet | READY |
| design-01 Long chats | READY |

**One blocker, and it is not in any unit.** The live guard cursor, `learn/data/cursor.json`, is stale
(below, X1). As it stands, the first message of the next sitting is blocked, and so is every message after
it. Clear it and code-01 can be taught today.

## 1. Automated checks

- `python3 check_unit.py units/*/*.md`: all 7 PASS.
- `python3 selftest.py`: 52 PASS, "all attacks caught", exit 0.
- `python3 scripts/check_teaching_system.py`: 67 of 67 pass.

They are green, but see M-S1: the selftest's engine checks do not test what their names say.

## 2. Blocker

**X1. The guard's live cursor says code-02 is half taught, so it blocks all of code-01.**
- `learn/data/cursor.json` holds `{"unit": ".../code/02-one-call-one-record.md", "index": 7, "finished": false}`.
- It was last changed at 16:44 today, before this sign-off started. Some earlier test walked code-02
  through the real guard without setting `FAIZ_CURSOR`.
- `hooks/teaching-guard.py:133-134` blocks any step from another unit while this cursor is unfinished.
  I ran code-01's Goal through the guard with a copy of the real cursor. Result: "The unit
  02-one-call-one-record.md is not finished". Because the cursor never moves on a block, every later step
  is blocked too.
- Nothing ever resets the cursor. `grep cursor` finds it in no hook, in the engine, in SKILL.md or in
  INTEGRATED.md.
- **Fix, now:** delete `learn/data/cursor.json`.
- **Fix, lasting:** (a) have `engine.py today` (or `predict`) reset the cursor when it names a new unit;
  (b) make the guard refuse to run from a test without `FAIZ_CURSOR`, or have every harness set it, so tests
  cannot write the live file.

## 3. Majors

**N1. code-02's Retry is answered by the Help block sent just before it.**
- SKILL.md step 5 says: after a miss, send the Help blocks, then the Retry.
- `Help: Your turn` says that `return reply` is quietly wrong (`02-one-call-one-record.md:486`). Its
  question and Key (l.488, l.495) cover `reply["output_tokens"]`, which is a crash.
- Retry B (l.529) and Retry C (l.533) are exactly those two slips. That makes 6 of the Retry's 10 parts
  pre-answered.
- **Fix:** give Retry B and C new slips, for example `reply["usage"]["output"]` (crash) and `return calls`
  (quietly wrong). Record the new runs and update the Key and the Worked answer (l.540-549).
- It has to be done before the next code sitting after a code-02 miss.
- code-01 does not have this problem: its Retry C (`tokens`) and D (`type`) are new slips.

**L1. llm-01 and llm-02 fail on any single slip.**
- Retry and Cold are 8 marks each, so 7 of 8 is 87.5%, under the 90 bar (`llm/01...md:372, 389`;
  `llm/02-tokens.md:352, 369`).
- In the sitting, three items of two half-marks each mean one missed half gives 83%.
- code (11 parts) and production (10 items) each allow one slip. The LLM units do not.
- **Fix, without changing the bar:** make Retry and Cold 10 marks, for example a one-line reason on items
  4 and 5. Log the in-session strictness for the 8-week bar review.

**M-G1. The guard lets an improvised question through if it lacks the answer marker.**
- The guard only inspects a message that contains `**Your answer.**` or ends with "your answer"
  (`teaching-guard.py:51`).
- "Quick check before we go on: what does print(row) show?" is allowed. The same question with the marker
  is blocked.
- Improvised questions are what broke session 1 (C49), so this is the hole that matters most. It is known
  and was accepted as the price of letting stuck-order messages through.
- **Fix:** when the cursor holds an unfinished unit, a non-step message containing "?" may only use
  question sentences found verbatim in that unit: its step bodies, its Keys' `Two options:` lines, or its
  Help blocks. Otherwise block it. SKILL.md should also say that stuck-order and prediction messages never
  carry the `**Your answer.**` marker. The eval review showed a `Two options` line sent with the marker is
  blocked. A stuck message that happens to end with "send your answer" is blocked too (tested).

**M-E1. The RETRY note in the engine tells the tutor to do what the guard forbids.**
- `engine.py:185` says "re-teach with its Help blocks and the show steps he missed".
- Re-sending a show step works only if no other unit has run since. Once llm-01 has run, the guard answers
  "A unit starts at its first step, not 'Show 3'" (tested, `teaching-guard.py:135-136`).
- SKILL.md step 5 says Help blocks only.
- **Fix:** change the engine note to "send its Help blocks for the missed steps, then its ## Retry item".

**M-S1. The selftest's engine checks pass for the wrong reason.**
- `selftest.py:155` copies `learn/` without `runs/`, and without the `../private` sources the units cite.
  In that copy every unit fails `check_unit`, so `done` is refused for that reason.
- So "engine refuses a result without a prediction" and "refuses a cold check before a session"
  (l.163-164) pass no matter what the prediction and cold logic do.
- There is still no retry flow in the selftest, which the last review asked for after B1.
- **Fix:** copy `runs/` and link `private`, `scripts` and `docs` into the temp root (as this sign-off did).
  Then add a flow: predict, miss, predict --retry, done --retry passed; and cold before and after its due
  date.

**M-P1 (not a teaching issue; the owner should know). ScaleDojo text is in public history.**
- The design-01 reviewer found that commit 43d45fb on origin/main still contains quoted ScaleDojo answer
  lines.
- The unit is now paraphrased and git-ignored, so it has no version control.
- A history rewrite, or accepting the exposure, is the owner's call.

## 4. Unit by unit

### code-01: READY after X1

**Review findings (`review-rebuild-code.md`), checked against the current file:**

| Finding | Status | Evidence |
|---|---|---|
| B1 Retry not recordable | FIXED | `engine.py` predict/done use the tag `:retry`; walked: miss 64, then predict --retry and done --retry records "passed" |
| M3 Show 3 overloaded | FIXED | split into Show 3 nesting (l.201-237), Show 4 list (l.269-311), Show 5 labels with one each of crash, quietly wrong and fine (l.349-391), plus Try with me 5, a label-only try (l.393-429) |
| M4 Show 2 overloaded | FIXED | tokens moved to Show 3 (l.205), indent explained (l.142), the "gone" row dropped, prompt is now a why question (l.154) |
| M5 re-teach blocked | PARTLY | Help: Your turn now stands alone on a new reply (l.654-694); the engine note is still wrong (M-E1) |
| M6 stuck moves 2 and 4 unprepared | FIXED | `Two options:` and `Worked answer:` in every try and scored Key (l.116-117, 197-198, 265-266, 345-346, 427-428, 476-477, 736-737, 779-780); the checker requires them |
| M7 cold bar | FIXED | 90 everywhere; a second cold is refused (walked) |
| M1, M2 checker trusts labels | PARTLY | `New:` is now split on `, ; / and`; there is still no taught-constructs check; only one Help block is required |
| minor 1 Retry repeats Help | FIXED for code-01 | see N1 |
| minor 2 `#` first met in the scored item | FIXED | taught in Show 5 (l.353) |
| minor 3 "call" before it is defined | FIXED | Goal l.20 no longer says "call"; l.24 is a forward pointer with a gloss |
| minor 4 "Steps on hand" under Try 1 | FIXED | removed |
| minor 5 read-back prompts | PARTLY | Show 2 (l.154) is still close to l.143 |
| minors 8, 9, 13, 16 (engine) | NOT FIXED | see section 6 |

**Simulation, all 12 steps as he gets them:**
- **Goal:** defines AI model, dict, key and value, with the bank-statement picture. The four steps get a
  forward gloss for "function". "Ready" is the only ask.
- **Show 1:** variable, `=`, lookup and trace table, all defined; `print` is glossed in line 2. The
  question is answerable from rows 2 and 3 of the table. About 95%.
- **Try 1:** one blank, a hinge question. About 90%. Option D says "crashes" before that word is taught
  (minor).
- **Show 2:** function, `def` with an indented body, argument, return value. Four small ideas grouped as
  three, with the bank-counter picture. About 2,000 characters. The heaviest Show, but every term is
  explained where it first appears.
- **Try 2:** two blanks. About 80 to 85%.
- **Show 3:** nesting, tokens, chained lookup. "Providers" is in the glossary's Taught list, so it is
  acceptable.
- **Try 3:** a four-option hinge. Each wrong option maps to a named mistake. About 80%.
- **Show 4:** list, `[0]`, the real shape, a dict written over several lines, `stop_reason`. That is four
  or five small ideas, a little over the cap, but each has one line.
- **Try 4:** about 80%.
- **Show 5:** comment and aim, crash, quietly wrong, fine, and the two-question test. Clear.
- **Try 5:** labels only. About 80%.
- **Your turn:** 11 parts at 90%, so one slip is allowed. Everything in it was modelled first.
- **Close:** a one-line rule.
- No sentence I would expect him to fail to parse.
- The Help blocks are all on new surfaces and stand alone.
- Retry D (`["type"]` shows the word `text`) is a fair but sharp trap.
- Cold is a new provider shape, and the path is given in words.
- Cards are correct, each with a second surface.

**Correctness:**
- Ran all 70 `learn/runs/code-01-*.json` programs with `.venv/bin/python`. Every output and exit code
  matches, file paths aside.
- The one caret-only difference is `code-01-predict.json`, from the voided session. No unit lists it.
- Opened turn_a to turn_d, retry_d, later_c, show1, show3, try1, try2, try3_check and try4_check: each
  matches the code and Keys in the unit.

### code-02: READY (from the sub-review; N1 verified by me)
- All 30 listed programs rerun and match. The Score lines add up to 10, 10 and 11. Every Worked answer is
  correct.
- Review findings fixed: B1, M6, M7, minor 6 (Help: Try with me 3 shows the whole program), minor 7
  (`first` explained, l.229).
- Partly fixed: M1, M2, M5 (engine note), minor 5 (read-back prompts at l.73 and l.236).
- Simulation: every Show adds 2 or 3 ideas. The Try questions come out at about 80 to 90%.
- Major N1, above.
- Minors:
  - "Steps on hand" under Try 2 (l.184) names a call and a return that Try 2 does not have.
  - Show 3's output (l.218) has no "It prints:" line.
  - The Retry Worked answer (l.549) does not spell out the list.

### llm-01 and llm-02: READY (sub-review; L1 verified by me)
- **Review findings fixed:**
  - M4: the Cold tested an untaught idea. The Karachi and Canberra pattern is now taught in the body, and
    the new Cold is Nigeria.
  - M5: llm-02's Retry evidence is now in the body.
  - Minors m1, m3, m4, m5, m6, m7 and m8.
- **Partly fixed:**
  - M3: the scoring is now 3 cases, but see L1.
  - m2: the How-it-works blocks are still 312 and 291 words.
- **Simulation:** llm-01's "One piece at a time" message has about 4 ideas. The other messages have 2 or
  3. Every question is answerable from what was shown.
- **Numbers:** 32 runs checked for llm-01 and all 46 for llm-02. All match.
- **Minors:**
  - Undefined: "LLMs" (llm-01 l.34), "internal numbers" (l.36), "chat training" (l.42).
  - Part 2 of each New case overlaps part 1.
  - llm-02 New case 3 should also accept " holding" as evidence (l.206).
  - llm-02 "the same price for every language" (l.113) is implied by facts.md, not stated as a row.

### production-01: READY (sub-review)
- **Review findings fixed:** M2 (the Help Keys re-send the step, and the guard allows it), m9, m10, m11,
  m12. M7 is partly fixed.
- **Two answers recomputed independently with exact fractions:**
  - Quick set 1 one call: 4,100 x $2/M + 500 x $10/M = $0.0082 + $0.005 = **$0.0132**.
  - Quick set 2: 2,000 x $5.94 = **$11,880**.
  - Retry: $0.0044 + $0.0025 = **$0.0069**.
  - All match the Keys, the runs, and the facts.md prices (Sonnet 5 at $2/$10, Haiku 4.5 at $1/$5).
- **Minor:** Quick set 1 item 5 (pricing on another model) is a new move under test, though the prices are
  given.

### evaluation-01: READY (sub-review)
- **Review findings fixed:** BL1, BL2, E-M1 (`floor: Fails caught=0.66`; always-PASS scores 9/15 and
  fails), E-M2, E-M3 (a food-facts step before the first try), E-M4, and m1 to m4 and m6.
- **Partly fixed:** S-M1, S-M2 (see M-G1), m5.
- **Not fixed, documented:** E-M6 (the clean pool is used up).
- **The engine's pass rule agrees with the unit's floor** for every possible set of batch scores.
- **Labels confirmed against the HW3 source:**
  - 46_25: FAIL, HIGH, paleo; "200g ... spaghetti".
  - 32_12: PASS, HIGH, gluten-free; almond and coconut flour.
  - 48_8: FAIL, HIGH; "crusty gluten-light bread".
  - All 47 cited trace IDs match the source labels.
- **Minors:**
  - l.126 "(a) the recipe or (b) the rule": both changed. Reword.
  - l.57 cites a step he has not seen yet.
  - Batch Worked answers (l.471, 535, 595, 658) cover all three items. Say "give only the stuck item's
    sentence".
  - l.781 "back to your recipe": say "recipe 1".
  - l.402 never states the 15.
  - Retry and Cold reuse taught surfaces.

### design-01: READY (sub-review, one verdict changed by me)
- **Review findings fixed:** BL1, D-M1, D-M2, D-M3 (content), D-M4, D-M7, and m7 to m10.
- **Partly fixed:** S-M1, S-M2, D-M5 (INTEGRATED l.81 still says "Design Crit"), D-M6 (see M-P1).
- **All 11 run numbers match** (1,550; 150; 4,000; 1,000; 1,300; 250; 350; 10,000; 70; 0.5).
- **The sub-reviewer called one issue a blocker; I rate it minor.** The issue: the design Retry and Cold
  never ask for a prediction, and `done --retry/--cold` refuses without one.
  - This is true of every unit except production and evaluation.
  - code-01 and the LLM units do not even ask for the first in-session prediction inside the unit.
  - SKILL.md steps 3, 5 and 6 tell the tutor to ask for the prediction.
  - The guard allows that plain question when it carries no answer marker (tested).
  - **Fix for all units:** add a one-line prediction message ("Before you start: predict your score, from
    0 to 100.") as a `## Retry` and `## Cold` opener, as evaluation does. Or state in SKILL.md the exact
    wording to use, without the marker.
- **Minors:**
  - l.133 "cuts" brings in dropping old messages, which the toolbox never taught.
  - l.252: with 1 / 0.5 / 0 marks, the 70 bar really needs 2.5 of 3. Say so before he predicts.

## 5. Engine walk (scratch copy, `.venv` python)

1. **today:** "Week 1, sitting 2 of 7", with code-01. It works, but it is his first sitting (old minor 16).
2. **done before predict:** refused, correctly.
3. **predict 85, tries 4 of 5, done `Your turn=0.82`:** "82, not yet, gap -3". The close line is queued.
4. **today:** offers llm-01 at once under the same "sitting 2" label.
   - In weeks 1 and 2 the plan is one new unit per sitting.
   - If the tutor runs `today` again after code-01, it looks like a second unit in the same sitting.
   - Minor: the tutor should end the sitting after one unit. The label should advance, or the output
     should say "next sitting".
5. **Miss path:** code-01 at 0.64, then llm-01 passed. The next `today` says "sitting 4 of 7" (the empty
   design slot is skipped) and "RETRY: re-teach with its Help blocks and the show steps he missed" (see
   M-E1).
6. **Retry without predict:** refused. With predict --retry, `Retry=0.5` gives "not yet".
   - A further `--retry` of 0.95 was accepted and recorded as passed. The engine allows any number of
     retries (old minor 8).
   - The "missed twice, prepare a parallel unit" route is reached only through `today`.
7. **Cold:** results backdated 8 days, and both colds are listed as due.
   - `done --cold` without a prediction is refused.
   - A cold of 0.73 gives "Cold check missed: at the start of the next code sitting, re-teach...". That
     note is printed once and never listed by `today` again (minor; M7 is only partly fixed).
   - A second cold is refused. llm-01's cold was recorded and passed.
8. **dashboard:** code 70 / 73 cold / gap 2; llm 97 / 100 / gap 24 with "ALARM prediction gap +24". It
   makes sense.
9. **Cards:** queued even after the failed session (old C13, still open), due the next day, and `due` is
   empty today. That is correct.

Nothing errored.

## 6. Guard walk (synthetic transcripts, temporary `FAIZ_CURSOR`)

| Case | Result |
|---|---|
| Every step of all 7 units, in order, with short feedback in front | all allowed |
| Stuck message "Look at your answer to question 2. Two options: ... Which one?" | allowed |
| Stuck message ending "Send your answer" | **blocked** (the regex anchors on the end; M-G1) |
| A Help block mid-unit | allowed |
| Re-sending step 1 of the same unit | allowed |
| Improvised question with `**Your answer.**` | blocked |
| Improvised question with no marker | **allowed** (M-G1) |
| After another unit finished, re-sending code-01 Show 3 for a RETRY | blocked (M-E1) |
| Help: Your turn, then Retry, in a later sitting | allowed |
| Cold sent mid-unit in sitting 1 | allowed (minor: should wait for its due date) |
| Feedback holding a `?` before a step | blocked, correctly |
| Feedback quoting a truncated Worked answer line before a step | allowed (the leak check needs the full Key line) |

## 7. Minors (brief)

- **Checker:**
  - Still trusts the author's `New:` count.
  - Needs only one Help block.
  - Does not check How-block content, whether the Retry copies Your turn, or card and cold answers
    against the shown text.
- **Engine:**
  - Unlimited retries.
  - A cold miss is not re-listed.
  - Cards are queued after a failed unit.
  - The sitting label skips numbers.
  - The docstring still says "Trace=0.8".
- **Guard:**
  - A Retry or Cold can be sent at any time.
  - The Key-leak check is exact-line only and runs only on marked messages.
  - "your answer" at the end of a stuck message blocks it.
- **Method, unchanged from earlier audits:**
  - Self-graded recall.
  - Close lines become self-graded cards.
  - Rungs are ignored.
  - No placement item.
- **Units:** as listed under each unit.

## 8. Before the next sitting

1. Delete `learn/data/cursor.json` (X1). Then code-01 is ready.
2. Change the RETRY note at `engine.py:185` to match SKILL.md (M-E1).
3. Decide how the prediction question is sent (the design-01 note). Adding a one-line opener to each unit's
   Retry and Cold is the cleanest option.
4. Before any code-02 or LLM Retry or Cold is served: N1 and L1.
5. When there is time: M-G1, M-S1 and the lasting fix for the cursor.
